# PoseMeme

Real-time pose and hand to meme matcher using MediaPipe Tasks Vision.

## Run Locally

Prerequisites: Node.js

1. Install dependencies:
   `npm install`
2. Start development server:
   `npm run dev`
3. Build for production:
   `npm run build`
